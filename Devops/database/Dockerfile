# database/Dockerfile
# Requirement: Minimal base image
FROM postgres:15-alpine

# Requirement: Configure mandatory credentials
ENV POSTGRES_DB=appdb
ENV POSTGRES_USER=appuser
ENV POSTGRES_PASSWORD=apppassword